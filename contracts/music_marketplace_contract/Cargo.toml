[package]
name = "music-marketplace-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20.0.0"

[profile.release]
opt-level = "z"
lto = true
overflow-checks = true
codegen-units = 1
